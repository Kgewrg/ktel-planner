library main_v2.globals;

String departureCity = "";
String destinationCity = "";

String day = "";

Duration minDelayTime = const Duration(hours: 0, minutes: 10);
Duration maxDelayTime = const Duration(hours: 1, minutes: 0);


// Δεν πρέπει αν είναι εδώ τα defaults
